#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main() {
    string s = "Hello";
    cout << (s.find('l')) << endl;

    vector<string> vec;

    string a;
    a += 'c';
    vec.push_back(a);
}
